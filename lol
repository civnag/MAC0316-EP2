title "lol";
variables
    sample of string s;
    sample of (float,float,float) g;
    string ss;
    string ss3;
end variables
commands
    if (1 < (2 + 4)) && (3 != 4) then
      print("Ok");
    else
      print("Not ok");
    end
